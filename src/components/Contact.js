import React, { Component } from 'react';

class Contact extends Component {
    state = {}
    render() {
        return (<section>
            this is Contact
        </section>);
    }
}

export default Contact;